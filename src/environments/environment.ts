export const environment = {
  production: false,
  // Replace with your actual Anthropic API key for Tab 2 (Claude Vision)
  // IMPORTANT: Never commit a real key — use environment variables or a proxy in production
  ANTHROPIC_API_KEY: 'YOUR_ANTHROPIC_API_KEY_HERE',
};
